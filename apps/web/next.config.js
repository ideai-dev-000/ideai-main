/**
 * @fileoverview Next.js configuration for the web application
 * 
 * @module WebNextConfig
 * @description
 * Next.js configuration file for the web application.
 * Currently uses default configuration, but can be extended
 * with custom settings as needed.
 * 
 * @example
 * This configuration is automatically loaded by Next.js
 * 
 * @see {@link https://nextjs.org/docs/app/api-reference/next-config-js} - Next.js config docs
 * @todo Add custom configuration as needed
 */

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
