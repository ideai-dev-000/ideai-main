/**
 * @fileoverview IdeaI Framework Atoms - Atomic Design Components
 *
 * @module IdeAIFrameworkAtoms
 * @description
 * Atomic components are the smallest building blocks of the design system.
 * They cannot be broken down further and are framework-agnostic at the component level,
 * but adapt their styling based on the framework prop.
 *
 * Atoms:
 * - Button: Interactive button element
 * - Input: Form input element
 * - Badge: Status indicator element
 *
 * @see {@link ./ideai-framework-button.tsx}
 * @see {@link ./ideai-framework-input.tsx}
 * @see {@link ./ideai-framework-badge.tsx}
 */

export { IdeAIFrameworkButton } from "./ideai-framework-button";
export { IdeAIFrameworkInput } from "./ideai-framework-input";
export { IdeAIFrameworkBadge } from "./ideai-framework-badge";
